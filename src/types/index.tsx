export type JobCompanyName = {
  comapany: string;
  role: string;
  date: string;
};
export type ClientsData = {
  logo: string;
  text: string;
};
export type SiderData = {
  image: string;
  url: string;
};
